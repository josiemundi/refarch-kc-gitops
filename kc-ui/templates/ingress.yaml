---
# Source: kc-ui/templates/ingress.yaml
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: release-name-kc-ui
  labels:
    app: kc-ui
    chart: kc-ui-1.0.0
    release: release-name
    heritage: Tiller
spec:
  rules:
    - host: kcontainer-itgtests.apps.green-with-envy.ocp.csplab.local
      http:
        paths:
          - path: /
            backend:
              serviceName: "kc-ui-service"
              servicePort: http
